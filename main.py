# Import Class
from Variable import Variable
from Boolean import Boolean
from Array import Array

# Variable (numerik, char, logika)
# Variable (Array -> index, acees, len, 2D)
# Operator ( + , - , x , /)
# Operator Assignment ( += )
# Operator Comparison ( == , != , > , <)
# Operator Logical ( and, or, not)
# Operator Bitwise ( &, | , ~)
# Conditional Statement ( IF , Else, Case )
# Looping (while, for, break, continue)
# Input ( input() )
# list ( [], index - )
# Menbahkan anggota list
# Menghapus anggota list
# Mengurutkan anggota list
# Membalik urutan
# Tupple sama dengan list
# 

input = str(input('Masukan nama'))


if input == 10:
    suhu = str(input('Kalo'))
else: 
    print("Error")


print(suhu)